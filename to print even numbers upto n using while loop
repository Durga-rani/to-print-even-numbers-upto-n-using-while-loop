//to print even numbers upto n using while loop
#include <stdio.h>

int main() 
{
    int i, n;

    
    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("All even numbers from 1 to %d are:\n", n);
     i = 2;
     while (i <= n) 
     {
        printf("%d\n", i);
        i += 2; 
    }

    return 0;
}
Output:
Enter the value of n: 9
All even numbers from 1 to 9 are:
2
4
6
8
